-- This contains all the metadata for each room ,including the entities which are placed in it.

return {
	{
		name="test_room",
		entities = {},
		width = 15,
		height = 10,
		centerX = 9,
		centerY = 4
	}
}